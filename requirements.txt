# --- Local LLM (Ollama) ---
ollama>=0.4.0                # Python client for your local Ollama server
                              # pull the model separately: `ollama pull qwen2.5:7b` (or similar)

# --- Gemini API (tier-2 escalation) ---
google-genai>=0.8.0          # official Gemini SDK
python-dotenv>=1.0.0         # load GEMINI_API_KEY from .env

# --- Structured output / schemas ---
pydantic>=2.8.0              # DiagnosisResult schema — works with both Ollama's JSON mode and Gemini

# # --- Embeddings + retrieval (not needed now) ---
# sentence-transformers>=3.0.0 
# numpy>=1.26.0
# faiss-cpu>=1.8.0             

# --- Data handling ---
PyYAML>=6.0.1
jsonlines>=4.0.0
pandas>=2.2.0

# --- Testing ---
pytest>=8.0.0
pytest-mock>=3.14.0

# --- Utility ---
tqdm>=4.66.0

# --- Optional: only if you fine-tune Qwen locally ---
# unsloth>=2024.9            # fast LoRA fine-tuning, good Qwen support, works on single GPU
# peft>=0.13.0               # if not using unsloth, standard LoRA library
# transformers>=4.45.0
# datasets>=2.21.0
# torch>=2.4.0               # match to your CUDA version